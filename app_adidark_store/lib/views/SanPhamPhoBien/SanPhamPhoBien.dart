import 'package:flutter/material.dart';


class SanPhamPhoBienScreen extends StatelessWidget {
  const SanPhamPhoBienScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(

      ),
      body: SingleChildScrollView(

      ),
    );
  }
}